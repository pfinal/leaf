<?php

namespace Entity;

use Leaf\BaseObject;
use PFinal\Database\ModelTrait;

/**
 * {{ tableComment }}
 *
{% for key, value in fieldType %}
 * @property {{ value }} ${{ key }}
{% endfor %}
 */
class {{ entityName }} extends BaseObject
{
    use ModelTrait;

    public static function labels()
    {
        return [
{% for key, value in attribute %}
            '{{ key }}' => '{{ value }}',
{% endfor %}
        ];
    }

//    /**
//     * @param bool $returnAll
//     * @return array|string
//     */
//    public function statusAlias($returnAll = false)
//    {
//        $map = [
//            self::STATUS_YES => '有效',
//            self::STATUS_NO => '禁用',
//        ];
//
//        if ($returnAll) {
//            return $map;
//        }
//
//        return isset($map[$this->status]) ? $map[$this->status] : '';
//    }
}